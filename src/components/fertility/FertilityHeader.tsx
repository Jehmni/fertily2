import { CardHeader, CardTitle } from "@/components/ui/card";

export const FertilityHeader = () => {
  return (
    <CardHeader>
      <CardTitle>Fertility Calendar</CardTitle>
    </CardHeader>
  );
};