
import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export const PostDialogTitle = () => {
  return (
    <DialogHeader>
      <DialogTitle>Share with the Community</DialogTitle>
    </DialogHeader>
  );
};
